/** Public paths — keep files small; component falls back to CSS if load fails */
export const ONBOARDING_ASSETS = {
  bg: '/onboarding/bg.jpg',
  glow: '/onboarding/glow.png',
  particles: '/onboarding/particles.png',
};
